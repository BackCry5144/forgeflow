# ForgeFlow

**버전**: 3.0  
**Phase**: 6 - Production Ready ✅  
**최종 업데이트**: 2025-12-01

AI 기반 UI 프로토타입 생성 및 산출물 자동화 시스템 (Google Gemini 1.5 Pro)

---

## 🎯 주요 기능

- ✨ **Step-by-Step Wizard**: 5단계 마법사로 화면 요구사항을 체계적으로 수집
- 🤖 **4-Stage LLM Generation**: 토큰 한계를 극복하는 4단계 분할 프롬프트 전략
- 📄 **자동 산출물 생성**: Word 설계서, 테스트 계획서, 사용자 매뉴얼 자동 생성
- 🔄 **Continuation 재시도**: 토큰 초과 시 자동 이어받기 (MAX_TOKENS 대응)
- 💾 **임시저장/복원**: Wizard 데이터 임시저장 및 복원
- ⏳ **실시간 진행률**: AI 생성 진행률 실시간 표시
- 🗄️ **Context Caching**: Redis 기반 시스템 프롬프트 캐싱 (API 비용 절감)
- 📊 **CSV 일괄 등록**: 메뉴 데이터 CSV 파일로 일괄 업로드

---

## 🚀 빠른 시작 (3가지 방법)

### 방법 1: Docker 배포 (권장) 🐳

```powershell
# 1. 환경 변수 설정
copy backend\.env.example backend\.env
# .env 파일을 열어 GOOGLE_API_KEY를 설정하세요

# 2. 빌드 및 배포 (자동)
.\deploy.ps1

# 3. 접속
# http://localhost:8000
```

### 방법 2: 수동 빌드 + Docker

```powershell
# 1. 환경 변수 설정
copy backend\.env.example backend\.env

# 2. Frontend 빌드
.\build.ps1

# 3. Docker 실행
docker-compose up -d

# 4. 접속
# http://localhost:8000
```

### 방법 3: 개발 모드 (로컬)

```powershell
# Terminal 1: PostgreSQL + Redis
docker-compose up postgres redis -d

# Terminal 2: Backend API
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
# .env 파일에서 GOOGLE_API_KEY 설정
uvicorn main:app --reload --port 8000

# Terminal 3: Frontend Dev Server
cd frontend
npm install
npm run dev
```

**접속 주소**:
- 개발 모드: http://localhost:5173 (Frontend Dev Server)
- 배포 모드: http://localhost:8000 (Python이 Frontend 서빙)
- API 문서: http://localhost:8000/docs

---

## 📦 프로젝트 구조

```
ForgeFlow_Lite/
├── backend/          # Python FastAPI 백엔드
│   ├── main.py              # 앱 엔트리포인트
│   ├── routers/             # API 라우터
│   │   ├── menus.py         # 메뉴 CRUD + CSV 임포트
│   │   ├── screens.py       # 화면 CRUD + 승인 + 피드백
│   │   └── ai.py            # AI 생성 API
│   ├── services/            # 비즈니스 로직
│   ├── models/              # SQLAlchemy 모델
│   ├── schemas/             # Pydantic 스키마
│   ├── utils/               # 유틸리티 (CSV 파서 등)
│   ├── static/              # Frontend 빌드 파일 (배포 시)
│   ├── requirements.txt     # Python 의존성
│   ├── Dockerfile           # Docker 이미지
│   └── .env.example         # 환경 변수 템플릿
│
├── frontend/                # React + Vite 프론트엔드
│   ├── src/
│   │   ├── pages/           # 페이지 컴포넌트
│   │   ├── components/      # UI 컴포넌트
│   │   ├── services/        # API 서비스
│   │   └── types/           # TypeScript 타입
│   ├── package.json         # Node 의존성
│   └── vite.config.ts       # Vite 설정
│
├── docker-compose.yml       # Docker Compose 설정
├── build.ps1                # Frontend 빌드 스크립트
├── deploy.ps1               # 전체 배포 스크립트
└── README.md                # 이 파일
```

---

## 📊 API 엔드포인트 (총 15개)

### 메뉴 관리 (5개)
- `POST /api/menus` - 메뉴 생성
- `POST /api/menus/import` - CSV 일괄 업로드
- `GET /api/menus` - 메뉴 목록 조회
- `GET /api/menus/{id}` - 메뉴 상세 조회
- `PUT /api/menus/{id}` - 메뉴 수정
- `DELETE /api/menus/{id}` - 메뉴 삭제

### 화면 관리 (6개)
- `POST /api/screens` - 화면 생성
- `GET /api/screens` - 화면 목록 조회 (menu_id 필터)
- `GET /api/screens/{id}` - 화면 상세 조회
- `PUT /api/screens/{id}` - 화면 수정
- `DELETE /api/screens/{id}` - 화면 삭제
- `POST /api/screens/{id}/approve` - 화면 승인
- `POST /api/screens/{id}/feedback` - 피드백 추가

### AI 생성 (3개)
- `POST /api/ai/generate` - 프로토타입 + 설계서 생성
- `POST /api/ai/regenerate` - 피드백 기반 재생성
- `POST /api/ai/generate-documents` - 산출물 생성 (테스트계획, 매뉴얼)

**상세 API 문서**: http://localhost:8000/docs

---

## 🔧 기술 스택

### Backend
- **Framework**: FastAPI 0.104.1
- **Language**: Python 3.11
- **Database**: PostgreSQL 17 Alpine
- **ORM**: SQLAlchemy 2.0
- **Validation**: Pydantic 2.5
- **AI**: Google Gemini 1.5 Pro (google-generativeai)
- **Cache**: Redis (Context Caching)
- **Document**: python-docx (Word 생성)

### Frontend
- **Framework**: React 18
- **Language**: TypeScript
- **Build Tool**: Vite 5
- **Styling**: TailwindCSS 3.3
- **UI Components**: Shadcn/ui
- **HTTP Client**: Axios
- **Routing**: React Router v6

### DevOps
- **Containerization**: Docker, Docker Compose
- **Database**: PostgreSQL (Docker)
- **Deployment**: Single Python server (배포 모드)

---

## 🧙 Step-by-Step Wizard 구조

ForgeFlow의 핵심 기능은 **5단계 Wizard**를 통해 화면 요구사항을 체계적으로 수집하는 것입니다.

### Wizard 5단계 구성

| 단계 | 이름 | 설명 | 수집 데이터 |
|:---:|:---|:---|:---|
| **1** | 화면 개요 | 기본 정보 입력 | `screenName`, `description` |
| **2** | 레이아웃 | 화면 구조 선택 | `selectedLayout`, `layoutAreas[]` |
| **3** | 컴포넌트 | 구성 요소 배치 | `components[]` (타입, 라벨, 영역) |
| **4** | 인터랙션 | 동작 정의 | `interactions[]` (트리거, 액션, 모달) |
| **5** | 검토 | 최종 확인 | 전체 데이터 리뷰 |

### 지원 레이아웃 타입
- `search-grid`: 검색 조건 + 데이터 그리드 (가장 일반적인 MES 화면)
- `master-detail`: 마스터 그리드 + 상세 뷰
- `dashboard`: 대시보드 레이아웃
- `kanban`: 칸반 보드
- `form`: 단순 폼 화면

### 지원 컴포넌트 타입
- **Form Controls**: `textbox`, `combo`, `checkbox`, `radio`, `date-picker`, `codeview`, `button`
- **Data Display**: `grid`, `chart`, `card`, `badge`, `progress-bar`
- **Special**: `codeview` (팝업 검색용 특수 컴포넌트)

### 지원 인터랙션 타입
- **Trigger Events**: `click`, `double-click`, `row-click`, `change`, `submit`
- **Actions**: `fetch-data`, `submit`, `clear`, `open-modal`, `validate`, `navigate`
- **Modal Types**: `form` (입력폼), `detail` (상세정보), `confirm` (확인대화), `custom`

---

## 🤖 4-Stage LLM Prompt 전략

### 문제 인식: LLM 토큰 한계

LLM은 1회 응답에서 출력할 수 있는 최대 토큰 수가 제한되어 있습니다 (예: Gemini 1.5 Pro = 8192 토큰).  
복잡한 React 컴포넌트(검색 영역 + 그리드 + 다수의 모달)를 한 번에 생성하면 **토큰 초과로 코드가 잘리는 문제**가 발생합니다.

### 해결 전략: 4단계 분할 생성 (ChatSession 기반)

ForgeFlow는 **ChatSession**을 유지하면서 **4단계로 프롬프트를 분할**하여 순차적으로 코드를 생성합니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                    4-Stage Generation Pipeline                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [Step 1/4] 기초 설정 및 유틸리티                                │
│  ─────────────────────────────────                              │
│  • React Hook 구조분해 (useState, useEffect, useCallback)       │
│  • Lucide 아이콘 SVG 정의                                        │
│  • sampleData (5건 이상)                                         │
│  • Modal, CodeView, Input 등 공통 컴포넌트                       │
│  • export default function ... { ← 여기까지만                    │
│                                                                  │
│  [Step 2/4] 상태 및 비즈니스 로직                                │
│  ─────────────────────────────────                              │
│  • State 정의 (searchParams, gridData, isModalOpen 등)          │
│  • Event Handlers (handleSearch, handleReset, openModal 등)     │
│  • UI 렌더링 코드는 아직 X                                       │
│                                                                  │
│  [Step 3/4] 메인 화면 UI 렌더링                                  │
│  ─────────────────────────────────                              │
│  • renderSearchArea() 함수                                       │
│  • renderGridToolbar() 함수                                      │
│  • renderGridArea() 함수                                         │
│  • 메인 return ( ... ) 시작                                      │
│  • 모달은 아직 X                                                 │
│                                                                  │
│  [Step 4/4] 모달 구현 및 최종 완성                               │
│  ─────────────────────────────────                              │
│  • 모든 Modal JSX 작성                                           │
│  • return ); 닫기                                                │
│  • 함수 } 닫기                                                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 프롬프트 설계 핵심 원칙

1. **명확한 경계 지정**: 각 단계가 어디서 끝나야 하는지 명시
2. **중복 방지 지시**: "앞 단계의 코드를 절대 반복하지 마세요"
3. **문맥 유지**: ChatSession을 통해 이전 출력을 LLM이 기억
4. **구조적 힌트**: `{ 여는 중괄호까지만`, `return ( 시작` 등 구체적 지시

### 코드 위치: `backend/utils/prompt_templates.py`

```python
# 4단계 프롬프트 생성 함수
get_step_1_prompt(wizard_data)  # 유틸리티, 아이콘, 공통 컴포넌트
get_step_2_prompt(wizard_data)  # 상태, 핸들러 로직
get_step_3_prompt(wizard_data)  # 메인 UI 렌더링
get_step_4_prompt(wizard_data)  # 모달 및 파일 완성
```

---

## 🔄 Continuation 재시도 메커니즘

### 문제: 단계 내에서도 토큰 초과

4단계로 분할해도, 복잡한 모달이 많은 경우 **단일 단계 내에서 토큰 초과**가 발생할 수 있습니다.  
이때 LLM은 `finish_reason = MAX_TOKENS (값: 2)`를 반환하며 응답이 잘립니다.

### 해결: 자동 Continuation (이어받기)

```python
async def _handle_step_continuation(self, chat_session, initial_response, step_name):
    """각 단계 내에서 토큰이 잘렸을 때 문맥을 유지하며 이어받기"""
    full_text = initial_response.text
    finish_reason = initial_response.candidates[0].finish_reason
    
    attempt = 0
    while finish_reason == 2 and attempt < self.max_continuation_attempts:  # 2 = MAX_TOKENS
        attempt += 1
        
        # 마지막 100자를 컨텍스트로 제공
        last_chars = full_text.strip()[-100:]
        continuation_prompt = (
            f"⚠️ SYSTEM: Previous response was truncated. "
            f"CONTINUE generating the code exactly from where it stopped:\n"
            f"...{last_chars}\n"
            f"(Do NOT repeat the context above. Just output the rest of the code.)"
        )
        
        response = await self._send_chat_with_retry(chat_session, continuation_prompt, ...)
        full_text += response.text  # 이어붙이기
        finish_reason = response.candidates[0].finish_reason
    
    return full_text
```

### Continuation 흐름도

```
┌──────────────────────────────────────────────────────────────┐
│                  Continuation Flow (예: Step 4)              │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  [Initial Request]                                            │
│       ↓                                                       │
│  LLM Response (1차)                                           │
│       ↓                                                       │
│  finish_reason == MAX_TOKENS (2)?                            │
│       ├─ No  → 완료, 다음 단계로                              │
│       └─ Yes → Continuation 시작                              │
│              ↓                                                │
│  "...마지막 100자 컨텍스트...                                 │
│   CONTINUE from where you stopped"                            │
│              ↓                                                │
│  LLM Response (2차) → 이어붙이기                              │
│              ↓                                                │
│  finish_reason 다시 체크                                      │
│       ├─ No  → 완료                                           │
│       └─ Yes → 최대 3회까지 반복                              │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 설정값

| 설정 | 값 | 설명 |
|:---|:---:|:---|
| `max_continuation_attempts` | 3 | 단계당 최대 이어받기 시도 횟수 |
| `max_quota_retries` | 10 | API 할당량 초과 시 재시도 횟수 |
| `retry_delay_seconds` | 60 | 할당량 대기 시간 (초) |

---

## 📡 API 할당량 재시도 (Quota Retry)

Google Gemini API는 분당 요청 수가 제한됩니다. 할당량 초과 시 `ResourceExhausted` 에러가 발생합니다.

### 자동 재시도 로직

```python
async def _send_chat_with_retry(self, chat_session, prompt, operation_name):
    for attempt in range(self.max_quota_retries + 1):
        try:
            response = await chat_session.send_message_async(prompt, ...)
            return response
        except google_exceptions.ResourceExhausted:
            if attempt < self.max_quota_retries:
                logger.warning(f"⏳ Quota exceeded. Retrying in 60s...")
                await asyncio.sleep(60)  # 1분 대기 후 재시도
            else:
                raise AIServiceError("quota_exceeded", "Quota exceeded after max retries")
```

---

## 💾 Context Caching (Redis)

### 목적: API 비용 절감

SYSTEM_PROMPT는 모든 요청에서 동일하게 사용됩니다.  
Redis에 캐싱하여 중복 전송을 방지하고 API 비용을 절감합니다.

### 캐싱 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                    Context Caching Flow                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. SYSTEM_PROMPT 해시 생성 (SHA256)                        │
│           ↓                                                  │
│  2. Redis에서 캐시 조회                                      │
│           ↓                                                  │
│     ┌─ HIT  → 캐시된 Context로 ChatSession 시작             │
│     └─ MISS → 새 ChatSession 생성 + Redis에 저장            │
│                                                              │
│  TTL: 1시간 (API 할당량에 맞춰 조정 가능)                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📄 자동 산출물 생성

### Word 설계서 생성 (`DocumentService`)

Wizard 데이터와 생성된 React 코드를 분석하여 Word 설계서를 자동 생성합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                   Document Generation Flow                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [Inputs]                                                    │
│  • wizard_data (5단계 수집 데이터)                           │
│  • prototype_html (생성된 React 코드)                        │
│  • screenshots (선택)                                        │
│            ↓                                                 │
│  [LLM Analysis]                                              │
│  • 코드에서 State, Handler, Layout 정보 추출                 │
│  • JSON 형식으로 구조화                                      │
│            ↓                                                 │
│  [Word Template Filling]                                     │
│  • {{SCREEN_NAME}}, {{DESCRIPTION}} 치환                     │
│  • {{SCREENSHOT}} 이미지 삽입                                │
│  • {{UI_STRUCTURE}} 트리 테이블 생성                         │
│  • {{USER_FLOW}} 동작 순서 테이블                            │
│  • {{TABLE:STATE}}, {{TABLE:EVENT}} 테이블                   │
│            ↓                                                 │
│  [Output]                                                    │
│  • Word 파일 (BytesIO)                                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ 데이터베이스 스키마

```sql
-- 메뉴 테이블
CREATE TABLE menus (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) UNIQUE NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 화면 테이블
CREATE TABLE screens (
    id SERIAL PRIMARY KEY,
    menu_id INTEGER REFERENCES menus(id),
    name VARCHAR(255) NOT NULL,
    description TEXT,
    prompt TEXT,
    prototype_html TEXT,
    design_doc TEXT,
    test_plan TEXT,
    manual TEXT,
    status VARCHAR(50) DEFAULT 'draft',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 피드백 테이블
CREATE TABLE feedback (
    id SERIAL PRIMARY KEY,
    screen_id INTEGER REFERENCES screens(id),
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🧪 테스트 방법

### 1. Health Check
```powershell
curl http://localhost:8000/health
# {"status": "healthy"}
```

### 2. CSV 업로드 테스트
```powershell
# test-menus-simple.csv 사용
curl -X POST http://localhost:8000/api/menus/import `
  -F "file=@test-menus-simple.csv"
```

### 3. 프로토타입 생성 테스트
```powershell
# 화면 생성
$body = @{
    menu_id = 1
    name = "로그인 페이지"
    description = "사용자 인증 화면"
} | ConvertTo-Json

curl -X POST http://localhost:8000/api/screens `
  -H "Content-Type: application/json" `
  -d $body

# AI 생성
$aiBody = @{
    screen_id = 1
    prompt = "이메일과 비밀번호 입력 폼, 로그인 버튼이 있는 화면"
    menu_name = "사용자 관리"
    screen_name = "로그인 페이지"
} | ConvertTo-Json

curl -X POST http://localhost:8000/api/ai/generate `
  -H "Content-Type: application/json" `
  -d $aiBody
```

---

## 📖 사용 가이드

### 1. 메뉴 등록
1. CSV 파일 준비 (`test-menus-simple.csv` 참고)
2. 메뉴 목록 페이지에서 "CSV 임포트" 클릭
3. 파일 업로드 → 메뉴 자동 생성

### 2. 화면 생성 및 프로토타입 생성
1. 메뉴 선택 → "화면 추가" 클릭
2. 화면 정보 입력 (이름, 설명)
3. 프롬프트 입력 (예: "로그인 폼이 있는 화면")
4. "프로토타입 생성" 클릭
5. 15-20초 대기 → HTML 프로토타입 + 설계서 생성

### 3. 피드백 및 재생성
1. 프로토타입 확인
2. 좌측 패널에서 피드백 입력 (예: "버튼 크기 키워주세요")
3. "재생성" 클릭 → 피드백 반영된 새 프로토타입 생성

### 4. 승인 및 산출물 생성
1. 프로토타입 만족 시 "설계 승인" 클릭
2. "산출물 생성" 클릭
3. 테스트 계획서, 사용자 매뉴얼 자동 생성
4. 각 문서 다운로드 (Markdown)

---

## ⚠️ 문제 해결

### Docker 컨테이너 시작 실패
```powershell
# 로그 확인
docker-compose logs -f

# 포트 충돌 확인 (8000, 5432)
netstat -ano | findstr :8000
netstat -ano | findstr :5432

# 컨테이너 재시작
docker-compose restart
```

### Frontend 빌드 실패
```powershell
# node_modules 재설치
cd frontend
Remove-Item node_modules -Recurse -Force
npm install
npm run build
```

### Database 연결 실패
```powershell
# PostgreSQL 컨테이너 상태 확인
docker-compose ps

# DB 로그 확인
docker-compose logs postgres

# DB 재시작
docker-compose restart postgres
```

### Google Gemini API 에러
- `.env` 파일에서 `GOOGLE_API_KEY` 확인
- API 키 유효성 확인: https://aistudio.google.com/apikey
- API 사용량 확인: https://console.cloud.google.com/apis
- 할당량 초과 시 자동 재시도 (최대 10회, 60초 간격)

---

## 📚 추가 문서

- [API 명세서](API_SPEC.md) - 전체 API 엔드포인트 상세
- [배포 가이드](DEPLOYMENT.md) - 프로덕션 배포 방법
- [사용자 가이드](USER_GUIDE.md) - 기능별 사용법
- [개발 계획](ForgeFlow_Lite_개발계획.md) - 전체 개발 로드맵

---

## 🏗️ 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             ForgeFlow Architecture                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Frontend (React + TypeScript)                │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                      │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │   │
│  │  │ MenuListPage │  │ MenuDetail   │  │ ScreenWorkspacePage      │  │   │
│  │  │              │  │ Page         │  │ ┌────────────────────┐   │  │   │
│  │  │ • CSV Import │  │ • 화면 목록  │  │ │ PrototypeWizard    │   │  │   │
│  │  │ • 메뉴 트리  │  │ • 화면 추가  │  │ │ (5-Step)           │   │  │   │
│  │  └──────────────┘  └──────────────┘  │ └────────────────────┘   │  │   │
│  │                                       │ ┌────────────────────┐   │  │   │
│  │                                       │ │ PreviewPanel       │   │  │   │
│  │                                       │ │ (React Preview)    │   │  │   │
│  │                                       │ └────────────────────┘   │  │   │
│  │                                       └──────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    │ Axios (REST API)                       │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Backend (FastAPI + Python)                   │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                      │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │   │
│  │  │ /api/menus  │  │/api/screens │  │ /api/ai/    │  │ /api/cache │ │   │
│  │  │             │  │             │  │ generate    │  │            │ │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └─────┬──────┘ │   │
│  │         │                │                │                │       │   │
│  │         ▼                ▼                ▼                ▼       │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │                       Services Layer                          │ │   │
│  │  ├───────────────────────────────────────────────────────────────┤ │   │
│  │  │  AIService           │ DocumentService  │ CacheService        │ │   │
│  │  │  • 4-Stage Prompt    │ • Word 생성      │ • Redis 캐시        │ │   │
│  │  │  • Continuation      │ • LLM 분석       │ • Context Cache     │ │   │
│  │  │  • Quota Retry       │ • Template Fill  │                     │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │                               │                                    │   │
│  │         ┌─────────────────────┼─────────────────────┐              │   │
│  │         ▼                     ▼                     ▼              │   │
│  │  ┌────────────┐       ┌────────────┐       ┌────────────┐          │   │
│  │  │ PostgreSQL │       │ Google     │       │   Redis    │          │   │
│  │  │ (DB)       │       │ Gemini API │       │   (Cache)  │          │   │
│  │  └────────────┘       └────────────┘       └────────────┘          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔮 향후 로드맵

### v3.1: AI 성능 최적화
- 더 정교한 프롬프트 튜닝
- 다양한 레이아웃 템플릿 추가
- 컴포넌트 라이브러리 확장

### v3.2: 사용자 경험 개선
- 실시간 프리뷰 Hot Reload
- 버전 히스토리 및 Diff 뷰
- 다중 화면 비교 기능

### v4.0: 고급 기능
- RAG (ChromaDB) 통합 - 기존 코드 학습
- PPT/Excel 산출물 지원
- 협업 기능 (실시간 편집)
- 다국어 지원

---

## 📚 관련 문서

- [LLM 시스템 개요](01.Docs/LLM_SYSTEM_OVERVIEW_ko.md)
- [프롬프트 전략 상세](01.Docs/Prompt_Steragy.md)
- [개발 계획](01.Docs/ForgeFlow_Lite_개발계획.md)
- [n8n Context Cache](01.Docs/n8n_ContextCache.md)
